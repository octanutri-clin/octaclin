# OctaClin - Matriz de confiabilidade e regressao

Atualizada na fase 3 do PR 52 da governanca de seguranca em 2026-09-04, sobre o
merge da fase 2 no `main` (`#194`). A cobertura,
as excecoes e os riscos residuais de acessibilidade tem matriz propria em
`docs/governance/matriz-acessibilidade.json`, validada por `pnpm test:a11y:matriz`.
Esta matriz conecta os riscos de maior
impacto aos testes automatizados existentes. Ela nao substitui o preflight,
smoke real de integracoes ou validacao manual de go-live.

| Risco | Protecao e teste | Comando principal | Gate |
| --- | --- | --- | --- |
| Isolamento multi-tenant | Pacientes e comunicacoes rejeitam IDs de outro tenant; agenda restringe por tenant e profissional. Testes: `octaclin-backend/src/modulos/pacientes/aplicacao/servico-pacientes.spec.ts`, `octaclin-backend/src/modulos/comunicacoes/aplicacao/servico-comunicacoes.spec.ts`, `octaclin-backend/src/modulos/agenda/aplicacao/servico-agenda.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Autenticacao e autorizacao | Login, lockout, recuperacao e permissoes negativas. Testes: `octaclin-backend/src/modulos/auth/aplicacao/servico-auth.spec.ts`, `octaclin-backend/src/modulos/auth/aplicacao/servico-protecao-abuso.spec.ts`, `octaclin-web/scripts/test-autorizacao-rotas.mjs`. | `pnpm --dir octaclin-web test:authz` | Bloqueia deploy | Desde 2026-08-18 o gate `test:authz` (que inclui `test:nutricao-plano`) e `test:apis-dinamicas` rodam no job web do CI; o segundo ainda se chamava `test:next15` quando entrou no CI. Antes eram apenas locais, e foi assim que uma mudanca de contrato chegou ao `main` sem sinal.
| Autorizacao de objeto e funcao | Matriz role x capability x recurso, carteira profissional nas mutacoes de comunicacoes, troca de painel exclusiva de SuperAdmin, revogacao de sessoes apos alteracao de acesso e rejeicao de campos HTTP nao declarados. Testes: `permissoes.spec.ts`, `servico-comunicacoes.spec.ts`, `servico-dashboard-clinico.spec.ts`, `servico-usuarios-cliente.spec.ts` e `pipe-validacao-http.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Sessoes e rotacao de refresh token | Access e refresh sao verificados com algoritmo, emissor, audiencia, tipo, tenant, sessao e claims obrigatorias; refresh e de uso unico, reuso revoga a familia e a revogacao vale entre instancias pelo Postgres. Testes: `octaclin-backend/src/modulos/auth/infraestrutura/configuracao-jwt.spec.ts`, `octaclin-backend/src/modulos/auth/dominio/claims-token.spec.ts`, `octaclin-backend/src/modulos/auth/apresentacao/guarda-jwt.spec.ts`, `octaclin-backend/src/modulos/auth/aplicacao/servico-sessoes.spec.ts`, `octaclin-backend/src/modulos/auth/aplicacao/sessoes-rotacao.integracao.spec.ts`, `octaclin-web/scripts/sessoes-bff.spec.ts`, `octaclin-web/tests/visual/sessoes-conta.spec.mjs`. | `pnpm --dir octaclin-backend test --runInBand` (a prova de concorrencia, RLS e multi-instancia exige Postgres real: `RLS_PROVA_BANCO_*` no CI ou `RLS_TESTCONTAINERS=true`), `pnpm --dir octaclin-web test:authz` e `pnpm --dir octaclin-web test:sessoes` | Bloqueia deploy |
| BFF e sessao | Cookies HttpOnly, API invalida, renovacao e rotas protegidas. Teste: `octaclin-web/scripts/smoke-e2e-bff.mjs`. | `pnpm --dir octaclin-web smoke:e2e:bff` | Smoke de staging/CI |
| Browser, BFF, CSRF, CORS, CSP e cache | Producao falha fechada sem cookie `Secure` ou allowlist HTTPS da API; mutacoes exigem origem valida e Fetch Metadata same-origin; CSP usa nonce por resposta sem `unsafe-inline` em scripts; paginas e BFF sensiveis recusam cache compartilhado; redirects hostis e XSS sao exercitados no navegador. Testes: `octaclin-web/scripts/seguranca-operacional.spec.ts`, `octaclin-web/scripts/validar-seguranca-runtime.mjs`, `octaclin-web/tests/visual/seguranca-browser.spec.mjs` e `octaclin-backend/src/main.spec.ts`. | `pnpm --dir octaclin-web test:seguranca-operacional`, `pnpm --dir octaclin-web test:seguranca-browser`, build, `pnpm --dir octaclin-web test:seguranca-runtime` e Jest focado do backend | Bloqueia deploy |
| Framework web e rotas dinamicas | Next.js, PostCSS e Sharp sem vulnerabilidades conhecidas; `params` dinamicos seguem o contrato assincrono do App Router. Teste: `octaclin-web/scripts/test-async-dynamic-apis.mjs`. | `pnpm --dir octaclin-web test:apis-dinamicas` e `pnpm --dir octaclin-web audit --prod` | Bloqueia deploy |
| Integracoes externas e OAuth | Google Calendar vincula state ao navegador com ticket/nonce de uso unico, HMAC, expiracao e PKCE; callback/redirect/token endpoint falham fechados; watch valida canal, recurso, profissional e expiracao; Google/Gmail/Meta recusam redirects e aplicam timeout; SMTP nao alcanca rede interna em ambiente fechado. Testes: `octaclin-backend/src/modulos/agenda/aplicacao/servico-conexao-google-calendar.spec.ts`, `octaclin-backend/src/modulos/agenda/aplicacao/servico-google-calendar.spec.ts`, `octaclin-backend/src/modulos/agenda/aplicacao/servico-sincronizacao-google-calendar.spec.ts`, `octaclin-backend/src/modulos/agenda/apresentacao/controlador-google-agenda.spec.ts`, `octaclin-backend/src/modulos/agenda/apresentacao/urls-google-agenda.spec.ts`, `octaclin-backend/src/modulos/comunicacoes/infraestrutura/adaptadores/adaptador-email-smtp.spec.ts` e `octaclin-backend/src/modulos/comunicacoes/infraestrutura/adaptadores/adaptador-whatsapp-meta.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy e exige smoke real controlado em staging |
| Webhooks e abuso publico | Meta exige HMAC sobre raw body, content type JSON, janela temporal e replay atomico; formularios e convites aplicam limites globais e por capability. Testes: `controlador-webhook-whatsapp.spec.ts`, `servico-protecao-abuso.spec.ts`, `controlador-formularios-publicos.spec.ts` e `controlador-convites-paciente.spec.ts`. | Backend Jest; `pnpm --dir octaclin-backend test:abuso:redis-real` contra Redis descartavel no CI | Bloqueia deploy |
| Concorrencia e idempotencia da agenda | Consulta e bloqueio manual compartilham trava transacional por tenant/profissional; Google usa ID deterministico e binding do evento; notificacao republica o mesmo ID; solicitacao publica tem referencia externa deterministica. Testes: `servico-agenda.spec.ts`, `servico-google-calendar.spec.ts`, `servico-sincronizacao-google-calendar.spec.ts`, `servico-agendamento-publico.spec.ts` e `test:fase253`. | Jest focado da Fase 253 e `pnpm --dir octaclin-web test:fase253` | Bloqueia deploy |
| Dados clinicos e portal | Escopo por profissional, portal do paciente, LGPD e rascunho publico versionado, limitado e sem credenciais no BFF. Testes: `octaclin-backend/src/modulos/pacientes/aplicacao/servico-portal-paciente.spec.ts`, `octaclin-backend/src/modulos/questionarios/aplicacao/servico-questionarios.spec.ts`, `octaclin-backend/src/modulos/questionarios/apresentacao/controlador-formularios-publicos.spec.ts`, `octaclin-web/tests/visual/portal-paciente.spec.mjs` e `octaclin-web/tests/visual/formulario-publico.spec.mjs`. | `pnpm --dir octaclin-backend test --runInBand` e Playwright direcionado | Bloqueia deploy |
| Recuperacao das superficies clinicas | Agenda, pacientes e prontuario sanitizam falhas HTTP, distinguem permissao e preservam formularios durante repeticao manual. Teste: `octaclin-web/tests/visual/fase-248-estados-recuperacao.spec.mjs`. | `pnpm --dir octaclin-web test:fase248` | Bloqueia deploy |
| Densidade e responsividade clinica | Agenda, pacientes e prontuario preservam controles de 44 px, teclado e ausencia de overflow em desktop/mobile. Teste: `octaclin-web/tests/visual/fase-249-densidade-responsividade.spec.mjs`. | `pnpm --dir octaclin-web test:fase249` | Bloqueia regressao visual |
| Operacao e recuperacao | Outbox, alertas, auditoria, LGPD e reprocessamento. Teste: `octaclin-backend/src/modulos/operacoes/aplicacao/servico-operacoes.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Equipe e comunicacoes | Troca de papel revoga sessoes e respeita tenant; canais operacionais nao expoem configuracao; falha de mensagem retorna estado persistido. Testes: `octaclin-backend/src/modulos/clientes/aplicacao/servico-usuarios-cliente.spec.ts`, `octaclin-backend/src/modulos/comunicacoes/apresentacao/controlador-comunicacoes.spec.ts` e `octaclin-web/tests/visual/fase-196-comunicacoes-equipe.spec.mjs`. | Backend Jest e Playwright direcionado | Bloqueia deploy |
| Modulos avancados | IA usa schemas fechados, contexto minimizado, configuracao sem fallback, limite por tenant/usuario e revisao humana obrigatoria; prompt/tool injection, saida arbitraria e paciente fora do escopo sao recusados antes de persistir. O FastAPI atual e local, sem provider ou ferramentas. Automacoes exigem simulacao; Mobile isola paciente e chave idempotente; Gamificacao e opt-in; Operacoes permanece SuperAdmin. Testes: `octaclin-backend/src/modulos/ia/aplicacao/servico-ia.spec.ts`, `octaclin-backend/src/modulos/ia/apresentacao/controlador-ia.spec.ts`, `octaclin-ai-service/tests/test_main.py`, `octaclin-backend/src/modulos/automacoes/aplicacao/servico-automacoes.spec.ts`, `octaclin-backend/src/modulos/mobile/aplicacao/servico-mobile.spec.ts`, `octaclin-backend/src/modulos/gamificacao/aplicacao/servico-gamificacao.spec.ts` e `octaclin-web/tests/visual/fase-197-modulos-avancados.spec.mjs`. | Backend Jest, `python -m unittest discover -s tests -v` no `octaclin-ai-service` e Playwright direcionado | Bloqueia deploy; provider externo permanece NO-GO |
| Tooling de agentes | Toda skill versionada e inventariada; helpers executaveis exigem capacidade minima e SHA-256; hooks recusam payload ambiguo, `.env`, paths proibidos e download remoto para shell; skills operacionais nao ficam no repositorio; scanner inclui `.agents`/`.claude`; Actions de IA no CI sao recusadas ate revisao explicita. Testes: `scripts/validar-tooling-agentes.spec.mjs` e `scripts/test-scan-secrets.mjs`. | `pnpm test:tooling-agentes`, `pnpm test:security` e `pnpm security:secrets` | Bloqueia merge no job de governanca |
| Containers e runtime (PR 50) | Bases fixadas por digest imutavel; estagio final non-root, sem gerenciador de pacotes e com healthcheck via runtime proprio. Prova estatica: `scripts/validar-dockerfiles-runtime.spec.mjs` (rejeita base sem digest, `latest`, secret e install no runtime). Prova de runtime real (Docker no CI): `scripts/harness-runtime-containers.sh` sobe cada imagem com `--read-only --cap-drop=ALL --security-opt=no-new-privileges` e limites de pids/memoria/cpu, provando uid efetivo non-root, escrita negada, canario de contexto ausente e history limpo; scan e SBOM cobrem a imagem final. Web e ai-service chegam a `healthy`; health do backend depende de config/provider (fronteira do PR 51). | `pnpm test:dockerfiles-runtime` (estatico) e o job `Imagem X (build e identidade)` do workflow Trivy (runtime real) | Bloqueia merge no job de governanca; runtime real bloqueia no CI, `SKIPPED` sem Docker nunca e `PASS` |
| Menor privilegio de providers (PR 51) | O isolamento entre tenants depende de a role do runtime nao ter `BYPASSRLS`: a policy de RLS nao e avaliada para uma role que o tem, e uma URL de owner colada no painel sobe sem erro nenhum. O processo real passa a medir, no bootstrap e sob demanda, se a role tem `SUPERUSER`/`BYPASSRLS`, se e membro de role privilegiada (alcancavel por `SET ROLE`), se tem `CREATE` no schema `public`, se o Redis usa TLS e se o endpoint de armazenamento e HTTPS. `nao-verificado` nunca conta como aprovacao e nenhum motivo carrega host, credencial ou nome de role. Testes: `octaclin-backend/src/infraestrutura/seguranca/menor-privilegio-providers.spec.ts` e `octaclin-backend/src/infraestrutura/seguranca/servico-menor-privilegio-providers.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand`; evidencia de producao por `GET /operacoes/providers` (SuperAdmin) e pela secao 6 de `docs/governance/POLITICA_PROVIDERS_MENOR_PRIVILEGIO.md` | Bloqueia deploy no teste; em staging e producao derruba o bootstrap por provider `violado` ou por `postgres` diferente de `conforme`, depois da medicao real de 2026-09-02 nos dois processos |
| Trilha de auditoria e redacao de metadados (PR 52, fase 1) | `user_action_logs.metadados` e `jsonb` sem schema, alimentada por dezenas de call sites, imutavel, exportada e sobrevivente ao expurgo do dado de origem. O redator apara credencial, PII e PHI por nome de chave e por formato de valor, preserva identificador opaco em forma de UUID, booleano de presenca e as duas excecoes de evidencia (`hashIntegridade`, `documentoLegal`), e nunca lanca. Como blocklist sobre payload livre nunca e completo, a cobertura e verificada e nao afirmada: o gate extrai as chaves realmente gravadas pelos call sites -- inclusive atraves dos envoltorios privados de auditoria, que sao declarados com arquivo, nome, indice do argumento, identificador repassado e justificativa -- e reprova chave descoberta, espalhamento de origem opaca (`...filtros`), escrita opaca sem envoltorio declarado, escrita da trilha fora dos dois caminhos autorizados e piso de sanidade do proprio extrator. Testes: `octaclin-backend/src/infraestrutura/auditoria/redacao-auditoria.spec.ts`, `octaclin-backend/src/infraestrutura/auditoria/servico-auditoria.spec.ts`, `scripts/validar-redacao-auditoria.spec.mjs`. | `pnpm test:redacao-auditoria` e `pnpm --dir octaclin-backend test --runInBand`; inventario por `pnpm audit:redacao-auditoria` | Bloqueia merge no job de governanca e bloqueia deploy no teste |
| Cobertura de eventos de auth e negativa de autorizacao (PR 52, fase 1) | Login, falha de login, renovacao de refresh e logout deixam rastro em `user_action_logs`; 403 de papel e de permissao deixam de ser mudo. Falha de login sem tenant ou usuario resolvido fica fora da trilha (so log estruturado), para nao dar a um anonimo escrita ilimitada em tabela imutavel sob RLS. A negativa deduplica por 60 s com o alvo concreto na identidade, grava `recurso_id` so em UUID canonico (a coluna e `uuid` e as guardas rodam antes dos pipes) e marca `alvoOpaco` quando o parametro nao e UUID, para que enumeracao de recursos nao colapse numa linha por minuto. Falha de auditoria nunca converte 403 em 500 nem em 200. Testes: `octaclin-backend/src/modulos/auth/apresentacao/auditoria-autorizacao.spec.ts`, `octaclin-backend/src/modulos/auth/apresentacao/guarda-papeis.spec.ts`, `octaclin-backend/src/modulos/auth/apresentacao/guarda-permissoes.spec.ts`, `octaclin-backend/src/modulos/auth/aplicacao/servico-auth.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Alerta e resposta a incidente de auditoria (PR 52, fase 3) | A trilha podia parar de gravar por horas sem alarme: o contador de falhas de `ServicoAuditoria` existia desde a fase 1 e nada em producao o lia. Passam a existir dois alertas no pipeline de `/operacoes` (atras de `SuperAdmin`, nunca em `/health/detalhado`, que e publico): falha de gravacao da trilha, `critico` a partir da primeira falha porque ausencia de registro e indistinguivel de ausencia de acesso; e volume de negativa de autorizacao, `atencao` a partir de 50 e `critico` a partir de 500, por total desde o boot ou por taxa por hora de uptime -- nunca por delta entre leituras, que o segundo leitor corromperia. Os dois contadores sao de processo, em variavel de modulo e nao em campo de instancia, porque `ServicoAuditoria` esta em `providers` de 15 modulos Nest e um campo de instancia daria 15 contadores; ha teste que reprova essa regressao. Nenhum dos dois alertas carrega identificador, rota ou alvo, e ha teste negativo sobre as chaves do payload. O procedimento de resposta, escalonamento, preservacao de evidencia, teste de alerta e o tabletop sintetico sao texto, e texto se perde em reescrita: o gate le o `RUNBOOK_PRODUCAO.md` e o tabletop e reprova a remocao das proibicoes que sao o controle, da condicao de encerramento sobre o intervalo sem trilha e a degradacao do tabletop em exercicio sem achado. Testes: `octaclin-backend/src/modulos/operacoes/aplicacao/servico-operacoes.spec.ts`, `octaclin-backend/src/modulos/auth/apresentacao/auditoria-autorizacao.spec.ts`, `scripts/test-runbook-resposta-auditoria.mjs`. | `pnpm test:resposta-auditoria` e `pnpm --dir octaclin-backend test --runInBand` | Bloqueia merge no job de governanca e bloqueia deploy no teste |
| Notificacoes in-app | Notificacao nao atravessa tenant nem escopo de profissional: destinatarios sao resolvidos por papel, evento sem dono nao vai para profissional nenhum, e leitura/marcacao filtram pelo `usuarioId` do JWT. Reentrega de webhook nao infla o contador. Testes: `octaclin-backend/src/modulos/notificacoes/dominio/destinatarios-notificacao.spec.ts`, `octaclin-backend/src/modulos/notificacoes/aplicacao/registrar-notificacao.spec.ts`, `octaclin-backend/src/modulos/notificacoes/aplicacao/servico-notificacoes.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Privacidade de notificacoes por papel | `Collaborator` recebe somente eventos operacionais; resposta de formulario clinico vai apenas para `SuperAdmin` e profissional responsavel. Testes: `destinatarios-notificacao.spec.ts` e `registrar-notificacao.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Exportacao CSV | Celula vinda de input do usuario nao vira formula na planilha da clinica (`=`, `+`, `@`, tab), e citacao/escape seguem uma unica implementacao para todas as exportacoes. Teste: `octaclin-backend/src/infraestrutura/exportacao/csv.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Importacao em massa de pacientes | Nenhuma linha do arquivo some sem relatorio: 200 linhas com 5 invalidas produzem 195 criados e as 5 acusadas pelo numero da linha **do arquivo original**, inclusive com campo multilinha. Reimportar o mesmo arquivo nao duplica ninguem. Testes: `servico-importacao-pacientes.spec.ts` e `infraestrutura/exportacao/csv.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Escopo de exportacao e importacao em massa | Exportacao sai da listagem que ja aplica o escopo (nunca de consulta paralela), o CSV da agenda nao carrega bloqueio do Google e Professional importa so para a propria carteira — o `profissionalResponsavelId` do corpo e ignorado. Testes: `servico-pacientes.spec.ts`, `servico-agenda.spec.ts`, `servico-importacao-pacientes.spec.ts`, `controlador-pacientes.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| TLS da conexao com o Postgres | A verificacao de cadeia e de hostname nao pode ser desligada em nenhum ambiente, e uma matriz de combinacoes de ambiente prova que nenhuma configuracao resolve para verificacao desligada; `sslmode` permissivo e TLS ausente reprovam em staging/producao; handshake real contra CA sintetica aceita o certificado confiavel e recusa CA incorreta, hostname incompativel e ausencia de ancora. A tag GCM tem tamanho fixo de 16 bytes na estrutura e na chamada ao Node. Testes: `octaclin-backend/src/infraestrutura/banco-dados/ssl-postgres.spec.ts` e `octaclin-backend/src/infraestrutura/banco-dados/ssl-postgres.handshake.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Envelope e rotacao da cifra de dados sensiveis | Escrita usa somente o envelope v1 com versao, key-id e AAD; ciphertext legado continua legivel; adulteracao em versao, key-id, IV, tag ou conteudo e payload truncado sao recusados; chave ausente ou curta derruba staging/producao; dual-read le a chave anterior e escreve so com a atual; a chave de cifra e distinta da chave do indice HMAC, cujo formato permanece identico ao ja gravado. Testes: `octaclin-backend/src/infraestrutura/seguranca/criptografia-envelope.spec.ts`, `octaclin-backend/src/infraestrutura/seguranca/criptografia-dados-sensiveis.spec.ts` e `octaclin-backend/src/infraestrutura/seguranca/ambiente-execucao.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Uploads e storage clinico | A inspecao do arquivo confirmado le a copia imutavel promovida (nunca o objeto pendente ainda reescrevivel pelo cliente); imagens tem largura/altura/pixels reais validados contra decompression bomb e metadado EXIF/GPS removido sem decodificar pixels; conteudo passa por inspecao antimalware que falha fechado em timeout/erro; exclusao so e considerada concluida apos HEAD confirmar ausencia no storage. Testes: `octaclin-backend/src/infraestrutura/armazenamento/servico-armazenamento-objetos.spec.ts`, `octaclin-backend/src/infraestrutura/armazenamento/sanitizacao-imagem.spec.ts`, `octaclin-backend/src/infraestrutura/armazenamento/servico-antimalware.spec.ts`, `octaclin-backend/src/modulos/mobile/aplicacao/servico-mobile.spec.ts`. | `pnpm --dir octaclin-backend test --runInBand` | Bloqueia deploy |
| Registro de migrations | A configuracao TypeORM referencia explicitamente as migrations clinicas e operacionais mais recentes. Teste: `octaclin-backend/src/infraestrutura/banco-dados/opcoes-typeorm.spec.ts`. | Backend Jest | Bloqueia deploy |
| Schema de producao atras do codigo | `/health/detalhado` marca `checks.migracoes` como falha quando ha migration pendente. Cobre o vao que o registro acima nao alcanca: aquele garante que a migration existe no codigo, este que ela foi aplicada no banco. Teste: `octaclin-backend/src/modulos/saude/servico-saude.spec.ts`. | Backend Jest e `curl /health/detalhado` apos cada deploy com migration | Bloqueia go-live da fase |
| Acessibilidade da web (rotas, estados e jornadas por teclado) | 33 rotas de console, portal e superficies publicas passam por axe-core (`wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa`), nome acessivel, foco visivel na tabulacao, padroes ARIA de modal/abas/menu e cinco jornadas completas somente por teclado. Teste: `octaclin-web/tests/visual/acessibilidade.spec.mjs`. | `pnpm --dir octaclin-web test:a11y`; no CI roda dentro de `pnpm --dir octaclin-web smoke:visual` | Bloqueia o job Demo local smoke |
| Reflow, zoom e reducao de movimento na web | Nove rotas continuam operaveis a 200% e 400% de zoom, em retrato e paisagem, com o espacamento de texto da WCAG 1.4.12 e sem rolagem em dois eixos; `prefers-reduced-motion` neutraliza animacoes. Teste: `octaclin-web/tests/visual/reflow-visual.spec.mjs`. | `pnpm --dir octaclin-web test:reflow`; no CI roda dentro de `pnpm --dir octaclin-web smoke:visual` | Bloqueia o job Demo local smoke |
| Matriz de acessibilidade afirmar cobertura inexistente | A matriz documental e confrontada com o codigo: script ausente do `package.json`, spec removido, bloco de teste renomeado, rota nao visitada, project do Playwright inexistente, gate desconectado do CI, resultado manual travestido de automatizado e leitor de tela nativo declarado PASS reprovam. Testes: `scripts/test-matriz-acessibilidade.spec.mjs` sobre `docs/governance/matriz-acessibilidade.json`. | `pnpm test:a11y:matriz` | Bloqueia o job Governanca de repositorio |
| Acessibilidade do aplicativo Expo | Controles do mobile mantem papel, nome, estado e alvo de toque de 44 pt; icone de fonte fica fora da arvore de acessibilidade nas tres plataformas; toda tela expoe cabecalho; a paleta preserva o contraste de texto e de limite de controle. Le codigo-fonte: impede regressao das correcoes do PR 33, **nao** valida o que TalkBack e VoiceOver falam. Teste: `octaclin-mobile/scripts/auditoria-acessibilidade.spec.mjs`. | `pnpm --dir octaclin-mobile test:a11y` | Bloqueia o job Mobile Expo do CI; distribuicao mobile segue NO-GO |
| PWA e dado clinico offline | Service worker limita cache a assets publicos; fila de check-in/formulario usa AES-GCM com chave apenas em memoria, idempotencia server-side e purge no logout/401. Testes: `octaclin-web/scripts/test-pwa-seguranca.mjs`, `octaclin-web/tests/visual/pwa-portal.spec.mjs`, specs de portal e questionarios. | `pnpm --dir octaclin-web test:pwa` e Jest focado do backend | Bloqueia deploy da PWA |
| Supply chain dos workflows | Actions remotas usam somente SHA completo de 40 caracteres e preservam a versao em comentario para atualizacao pelo Dependabot; tags, branches, SHA abreviado e pin sem versao documentada reprovam. Teste: `scripts/validar-actions-imutaveis.spec.mjs`. | `pnpm test:actions-imutaveis` | Bloqueia o job Governanca de repositorio |
| Injecao em scripts de deploy | Expressoes do GitHub Actions nao podem ser interpoladas diretamente em blocos `run`; inputs, matriz e secrets entram por `env` e sao usados como variaveis entre aspas. Payloads sinteticos com quebra de aspas, substituicao de comando e crases reprovam. Teste: `scripts/validar-workflows-sem-injecao.spec.mjs`. | `pnpm test:workflows-seguros` | Bloqueia o job Governanca de repositorio |
| Triagem factual de seguranca | O snapshot canonico cobre cada alerta CodeQL, Semgrep, Trivy e Dependabot exatamente uma vez; duplicatas apontam para um alerta primario e achados confirmados exigem fonte, sink, pre-condicoes, mitigacoes, impacto, severidade e PR de remediacao. Teste: `scripts/validar-triagem-seguranca.spec.mjs` sobre `docs/governance/triagem-seguranca-pr37.json`. | `pnpm test:triagem-seguranca` | Bloqueia o job Governanca de repositorio |
| Versao do package manager | Uma unica versao exata de pnpm em `package.json` dos quatro componentes, `PNPM_VERSION` do CI e `corepack prepare` dos Dockerfiles; major solto, divergencia entre fontes e `corepack enable` sem versao fixada reprovam. Teste: `scripts/validar-versao-pnpm.spec.mjs`. | `pnpm test:versao-pnpm` | Bloqueia o job Governanca de repositorio |
| Lifecycle script de dependencia e instalacao congelada | Executa o pnpm real contra fixtures sinteticas servidas por registry local efemero: dependencia com lifecycle nao aprovada reprova a instalacao e nao executa o script, dependencia aprovada instala e executa, manifest divergente do lockfile reprova o modo congelado. Tambem verifica que nenhum `pnpm-workspace.yaml` liga `dangerouslyAllowAllBuilds` ou `trustLockfile`, nem desliga integridade do store. Testes: `scripts/validar-instalacao-congelada.spec.mjs`, `scripts/registry-fixture-local.mjs`. | `pnpm test:instalacao-congelada` | Bloqueia o job Governanca de repositorio |
| Excecoes de supply chain | Toda excecao exige owner, justificativa util, alcance, controle compensatorio e prazo de no maximo 180 dias; excecao vencida, com curinga amplo em versao ou com id duplicado reprova. Cada `trustPolicyExclude` precisa de excecao correspondente. Vulnerabilidades aprovadas geram o `.trivyignore.yaml` efemero com ids, caminhos e vencimento vindos do mesmo ledger; supressao ampla ou paralela nao e aceita. Teste: `scripts/validar-excecoes-supply-chain.spec.mjs` sobre `docs/governance/excecoes-supply-chain.json`. | `pnpm test:excecoes-supply-chain` | Bloqueia o job Governanca de repositorio e alimenta o workflow Trivy |
| Runtime minimo dos containers | O estagio final dos tres Dockerfiles precisa declarar `USER` diferente de root e `HEALTHCHECK` usando apenas o runtime ja presente. O workflow constroi cada imagem e inspeciona `Config.User` e `Config.Healthcheck`, evitando aprovar apenas texto estatico. | `pnpm test:dockerfiles-runtime` e job `Imagem ... (build e identidade)` | Bloqueia o job Governanca de repositorio e o workflow Trivy |
| Licencas de dependencias | Classificacao SPDX por token com semantica de `OR`/`AND` (nunca substring, que confunde GPL com LGPL e AGPL); licenca bloqueada reprova, licenca de revisao obrigatoria exige revisao registrada para aquele pacote e licenca desconhecida exige excecao datada. O inventario real instalado de backend, web e mobile e auditado no CI. Testes: `scripts/validar-licencas.spec.mjs` sobre `docs/governance/politica-licencas.json`. | `pnpm test:licencas` e `node scripts/validar-licencas.mjs node_modules` em cada app | Bloqueia o job Governanca de repositorio e os jobs de app |
| Reprodutibilidade do lock Python | O lock do AI service traz todo o grafo transitivo em versao exata com hash, coerente com as dependencias diretas e com cabecalho de origem; requisito sem hash, versao frouxa, direta ausente ou versao divergente reprovam. No CI, hash adulterado precisa reprovar `pip install --require-hashes`. Teste: `scripts/validar-lock-python.spec.mjs` sobre `octaclin-ai-service/requirements.lock.txt`. | `pnpm test:lock-python` | Bloqueia o job Governanca de repositorio e o job AI FastAPI |
| Reproducao e cobertura do SBOM | Duas execucoes do Trivy sobre o mesmo commit precisam gerar inventario semantico identico apos normalizar `serialNumber` e timestamp; diferenca de pacote, versao, PURL, relacao ou licenca reprova. A cobertura exige um componente conhecido de backend, web, mobile e AI service, para detectar o sumico de um ecossistema inteiro. Teste: `scripts/validar-sbom.spec.mjs`. | `pnpm test:sbom` e `node scripts/validar-sbom.mjs sbom.cyclonedx.json sbom.cyclonedx.repeticao.json` no workflow Trivy | Bloqueia o job Governanca de repositorio e o job Trivy |
| Risco novo de dependencia no PR | `actions/dependency-review-action` compara base e head e reprova `critical/high` novo ou licenca da lista bloqueada introduzida pelo PR, sem transformar divida historica ja triada em bloqueio cego. | Workflow `Dependency Review` em `pull_request` | Bloqueia o PR |

## Execucao minima

```powershell
pnpm test:confiabilidade
pnpm test:redacao-auditoria
pnpm test:resposta-auditoria
pnpm test:a11y:matriz
pnpm test:actions-imutaveis
pnpm test:workflows-seguros
pnpm test:triagem-seguranca
pnpm test:versao-pnpm
pnpm test:excecoes-supply-chain
pnpm test:licencas
pnpm test:lock-python
pnpm test:sbom
pnpm test:instalacao-congelada
pnpm --dir octaclin-backend test --runInBand
pnpm --dir octaclin-web lint
pnpm --dir octaclin-web typecheck
pnpm --dir octaclin-web test:apis-dinamicas
pnpm --dir octaclin-web test:authz
```

Antes de producao, executar tambem `pnpm test:e2e:criticas`, validar
`/health/detalhado` e realizar uma acao real controlada para cada integracao
habilitada.
